services:
  - type: web
    name: silver-bullet
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python src/main.py
    rootDir: src
    autoDeploy: true